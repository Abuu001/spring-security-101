spring:
  profiles: test
  datasource:
    password: root
    url: jdbc:postgresql://localhost:5432/springsecurity
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    show-sql: true
